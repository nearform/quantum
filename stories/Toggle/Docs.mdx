import { Canvas, Meta } from '@storybook/blocks'

import * as ToggleStories from './Toggle.stories'

<Meta of={ToggleStories} />

# Toggle

A toggle is a component made of a switch with an associated label, alongside an additional icon that can be used for functionality such as removing or renaming a toggle element.

## Default

<Canvas of={ToggleStories.Default} />

## Attributes

<table>
  <tbody>
    <tr>
      <th>Attribute</th>
      <th>Type</th>
      <th>Default</th>
    </tr>
    <tr>
      <td>defaultChecked</td>
      <td>`boolean`</td>
      <td>-</td>
    </tr>
    <tr>
      <td>checked</td>
      <td>`boolean`</td>
      <td>-</td>
    </tr>
    <tr>
      <td>onCheckedChange</td>
      <td>`function`</td>
      <td>-</td>
    </tr>
    <tr>
      <td>disabled</td>
      <td>`boolean`</td>
      <td>-</td>
    </tr>
    <tr>
      <td>required</td>
      <td>`boolean`</td>
      <td>-</td>
    </tr>
    <tr>
      <td>label</td>
      <td>`string`</td>
      <td>-</td>
    </tr>
    <tr>
      <td>value</td>
      <td>`string`</td>
      <td>"on"</td>
    </tr>
  </tbody>
</table>

## Example

```tsx
import { Toggle } from '@nearform/quantum'
import DarkModeIcon from 'assets/icons'

const ToggleItem = () => {
  return (
    <Toggle label="Dark Mode" onCheckedChange={toggleDarkMode}>
      <DarkModeIcon />
    </Toggle>
  )
}
```
