import { Canvas, Meta } from '@storybook/blocks'

import * as CheckboxStories from './Checkbox.stories'

<Meta of={CheckboxStories} />

# Checkbox

A checkbox is a small box that users can click or tap to select or deselect an option. It's commonly used to allow users to make multiple choices from a list of options and is often accompanied by a label describing the option.

## Unchecked
<Canvas of={CheckboxStories.Unchecked} />

## Checked
<Canvas of={CheckboxStories.Checked} />

## Indeterminate
<Canvas of={CheckboxStories.Indeterminate} />

## With label
<Canvas of={CheckboxStories.WithLabel} />

## With label and hint
<Canvas of={CheckboxStories.WithLabelAndHint} />

## With label on the left
<Canvas of={CheckboxStories.WithLabelOnTheLeft} />

## Attributes
<table>
  <tbody>
    <tr>
      <td>Attribute</td>
      <td>Type</td>
      <td>Default</td>
    </tr>
    <tr>
      <td>checked</td>
      <td>`true | false | "indeterminate"`</td>
      <td>-</td>
    </tr>
    <tr>
      <td>defaultChecked</td>
      <td>boolean</td>
      <td>-</td>
    </tr>
    <tr>
      <td>disabled</td>
      <td>boolean</td>
      <td>-</td>
    </tr>
    <tr>
      <td>required</td>
      <td>boolean</td>
      <td>-</td>
    </tr>
    <tr>
      <td>name</td>
      <td>`string`</td>
      <td>-</td>
    </tr>
    <tr>
      <td>value</td>
      <td>`string`</td>
      <td>-</td>
    </tr>
  </tbody>
</table>

You can control the styling by using the Data-attributes in the className. For example, if you wanted to change the colour of the tick to yellow, then you can write:

```ts
<Checkbox checked=true className="data-[state=checked]:text-yellow-200 data-[state=checked]:bg-background-inverse data-[state=checked]:border-grey-900">
```

And you can see the change:

<Canvas of={CheckboxStories.ColourChange} />

You can do the same for the indeterminate style by using `data-[state=indeterminate]`.
Note that the colour of the SVG's stroke and fill (in this case, a tick or a minus) is inherited from the parents text colour.
