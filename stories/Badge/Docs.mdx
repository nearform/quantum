import { Canvas, Meta } from '@storybook/addon-docs/blocks'

import * as BadgeStories from './Badge.stories'

<Meta of={BadgeStories} />

# Badge

A badge labels the thing beside it — a status, a category, a count. It is
static: nothing about it responds to a click. A tag that can be pressed,
toggled or removed is a [Chip](?path=/docs/components-chip--docs).

### Default

The text inside the badge is the label. With no `variant` it takes the neutral
surface tokens, which is the right choice when the badge is categorising
rather than reporting a state.

<Canvas of={BadgeStories.Default} />

### Variants

`info`, `success`, `warning` and `error` tint the badge to reinforce what the
text already says. They do not replace it — see Accessibility below.

<Canvas of={BadgeStories.Variants} />

### Sizes

<Canvas of={BadgeStories.Sizes} />

### Pill

`shape="pill"` fully rounds the badge. It suits a single word or a count;
`rounded`, the default, suits longer labels.

<Canvas of={BadgeStories.Pill} />

### With a dot

`dot` puts a filled circle before the text in the badge's own colour, for the
online/away/offline kind of status. It is decorative, so the word beside it
still has to carry the meaning.

<Canvas of={BadgeStories.WithDot} />

### With an icon

`icon` takes any node — size it yourself, so it can match the surrounding
text. It is hidden from assistive technology.

<Canvas of={BadgeStories.WithIcon} />

### As a count

A bare number says nothing on its own. Give the badge an `aria-label` and it
announces that instead of the digits.

<Canvas of={BadgeStories.Count} />

## Dark mode

Every variant ships its own dark pairing, so nothing extra is needed — toggle
the theme in the toolbar to see it.

The tinted variants are a `-700` foreground on a `-100` surface in light mode
and a `-300` on a `-900` in dark. Those weights are deliberate: the `-600` on
`-100` pairing that most design systems reach for first falls below AA on four
of the Quantum ramps, and `-700` clears it on all of them. The neutral variant
takes the `background-alt` and `foreground` tokens rather than a grey ramp,
because `grey-900` on the dark page background is 1.19:1 and would leave no
visible badge at all.

## Accessibility

- **The text is the status; the colour only reinforces it.** A green badge
  reading "Live" and a red one reading "Stopped" are distinguishable to
  everyone; two identically-worded badges in different colours are not
  (WCAG 1.4.1). The same goes for `dot`, which is decorative.
- **A badge that is not self-explanatory takes an `aria-label`.** `3` on its
  own is meaningless out of context, so `aria-label="3 unread"` replaces it.
  Doing that also gives the badge `role="img"`, which is what makes the label
  reach a screen reader — `aria-label` on a plain `<span>` has no role to name
  and most screen readers ignore it.
- **A badge that changes while the page is open needs announcing.** Pass
  `role="status"` and the new text is read out when it changes. Your `role`
  wins over the generated one, with one exception: `role="img"` without a name
  to go with it is dropped. That role replaces the badge's text with its
  accessible name, so unnamed it fails WCAG 4.1.2 and puts the text out of
  reach at the same time -- there is no reading of it that is not a mistake.
  Pass an `aria-label` with it and it is kept.
- Icons are always `aria-hidden`, so an icon-only badge is silent. Give it an
  `aria-label` too.

## Attributes

<table>
  <tbody>
    <tr>
      <th>Attribute</th>
      <th>Type</th>
      <th>Default</th>
    </tr>
    <tr>
      <td>variant</td>
      <td>`"default" | "info" | "success" | "warning" | "error"`</td>
      <td>`"default"`</td>
    </tr>
    <tr>
      <td>size</td>
      <td>`"sm" | "default" | "lg"`</td>
      <td>`"default"`</td>
    </tr>
    <tr>
      <td>shape</td>
      <td>`"rounded" | "pill"`</td>
      <td>`"rounded"`</td>
    </tr>
    <tr>
      <td>icon</td>
      <td>`React.ReactNode`</td>
      <td>-</td>
    </tr>
    <tr>
      <td>dot</td>
      <td>`boolean`</td>
      <td>`false`</td>
    </tr>
  </tbody>
</table>

## Usage

```tsx
import { Badge } from '@nearform/quantum'

// A status, with the word doing the work
<Badge variant="success">Published</Badge>

// A state, with a dot
<Badge variant="warning" shape="pill" dot>Away</Badge>

// A count, named for a screen reader
<Badge variant="error" shape="pill" size="sm" aria-label="3 unread">
  3
</Badge>
```
